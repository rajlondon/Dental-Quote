import { Router } from 'express';

/**
 * Canonical Treatment Lines Router
 * This router handles all operations related to treatment lines in the V1 API.
 */
declare const router: Router;

export default router;