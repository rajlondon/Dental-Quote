import React from 'react';

/**
 * This component was previously used for translation reloading.
 * Now simplified since translations have been removed.
 */
const ReloadTranslations: React.FC = () => {
  // Component no longer needed for translations
  // Keeping as placeholder for future notification system if needed
  return null;
};

export default ReloadTranslations;